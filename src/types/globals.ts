export type Aligment = "center" | "centerleft" | "centerright"| "centertop" |"centerbottom" | "topleft" | "topright" | "bottomleft" | "bottomright";

export type Vector = {x: number, y: number};
export type LootType = {
  isOpen: boolean
}